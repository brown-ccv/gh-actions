{
  "name": "check-wait-label",
  "version": "1.0.0",
  "description": "Given a duration in days, apply a label to a PR of waiting or wait complete",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "package": "ncc build $npm_package_main -o dist"
  },
  "author": "mcmcgrath13",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.9.1",
    "@actions/github": "^2.0.0"
  },
  "devDependencies": {
    "@zeit/ncc": "^0.20.5"
  }
}
