{
  "name": "get-pr-type",
  "version": "1.0.0",
  "description": "Get the types of changes in a PR",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "package": "ncc build $npm_package_main -o dist"
  },
  "author": "mcmcgrath13",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.2.6",
    "@actions/github": "^2.0.0"
  },
  "devDependencies": {
    "@zeit/ncc": "^0.20.5"
  }
}
